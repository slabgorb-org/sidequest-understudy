"""Headless AI companion — joins a live SideQuest session as its own full-PC
seat over the WebSocket protocol. Ships to players. An in-tree sibling package
of seat_core (which it reuses) and understudy (which it must NEVER depend on:
the companion is not a test harness)."""
